{
	"name": "vivenu-event-monitor",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"dev:dashboard": "wrangler dev --env development && echo 'Dashboard available at http://localhost:8787/dashboard'",
		"start": "wrangler dev",
		"test": "vitest --run",
		"test:availability": "python ../test_availability.py --all",
		"test:dashboard": "python ../test_availability.py",
		"test:frankfurt": "node test_frankfurt_charity.js",
		"deploy:staging": "wrangler deploy --env staging",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.32.0"
	}
}
